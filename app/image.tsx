import { View, Text } from "react-native";
import React from "react";

const Image = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Image</Text>
    </View>
  );
};

export default Image;
